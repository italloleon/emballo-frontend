services:
  frontend:
    build:
      context: .
      args:
        VITE_API_URL: ${VITE_API_URL}
        VITE_ALLOW_REGISTRATION: ${VITE_ALLOW_REGISTRATION:-true}
    ports:
      - "5173:80"
    restart: unless-stopped
